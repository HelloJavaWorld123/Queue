server:
  port: 18802
logging:
  level:
    org:
      springframework:
        retry: debug
        amqp: debug
        transaction: debug
    com:
      rabbitmq: debug
spring:
  profiles:
    active: local
  application:
    name: rabbit_consumer_one
